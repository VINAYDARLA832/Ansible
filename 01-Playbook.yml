- name : install nginx
  hosts: frontend
  become: yes
  tasks:
    - name: install nginx task
      ansible.builtin.yum:
        name: nginx
        state: present
    - name: start nginx service
      ansible.builtin.service:  
        name: nginx
        state: started
        enabled: yes